use {npm create vite@latest}to make a new folder in react using terminal after that create project name and then choose
framework{react}varient{javascript}latent{ESLint}
After that go to that project folder using cd and then write {npm run dev} in terminal
then it give a localhost and easily we run that project in browser using this localhost
